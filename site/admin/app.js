/* VIP NETWORK ADMIN — updated: 5-minute secure session + history back + ADMIN+TV PWA */
const DEFAULT_WORKER_API=(window.VIP_ADMIN_WORKER_API||window.location.origin);
const seed=[{name:'Somoy TV',category:'News',logo:'',url:'https://example.com/somoy.m3u8',status:'Active'},{name:'Jamuna TV',category:'News',logo:'',url:'https://example.com/jamuna.m3u8',status:'Active'},{name:'ATN News',category:'News',logo:'',url:'https://example.com/atn.m3u8',status:'Active'}];
let WORKER_API=DEFAULT_WORKER_API,connected=false,channels=[],categories=['News','Entertainment','Sports','Kids','Movies','Music','Other'],selected=null,dashPage=1,managerPage=1;
const PAGE_SIZE=10;

/* VIP NETWORK LUXURY UI PATCH — layout only, preserves all existing functionality */
function injectLuxuryAdminUI(){
  if(document.getElementById('vipLuxuryAdminStyle')) return;
  const css=`
/* ===== VIP NETWORK LUXURY ADMIN LOGIN ===== */
#loginModal{background:radial-gradient(circle at 50% 12%,rgba(0,140,255,.16),transparent 34%),linear-gradient(135deg,#010913,#03182a 52%,#010a15)!important;backdrop-filter:blur(10px);z-index:99999!important;}
#loginModal .login-card,#loginModal .modal-card,#loginModal>div{background:linear-gradient(145deg,rgba(3,18,35,.97),rgba(1,10,22,.98))!important;border:1px solid rgba(0,174,255,.72)!important;border-radius:28px!important;box-shadow:0 0 0 1px rgba(0,110,255,.12),0 0 55px rgba(0,132,255,.20),inset 0 1px 0 rgba(255,255,255,.06)!important;}
.vip-lux-login{width:min(560px,calc(100vw - 28px));margin:auto;padding:30px 28px 24px;position:relative;overflow:hidden;text-align:center;color:#eaf6ff;border-radius:28px;}
.vip-lux-login:before{content:"";position:absolute;inset:-30% -20% auto;height:230px;background:radial-gradient(circle,rgba(0,153,255,.20),transparent 68%);pointer-events:none}
.vip-login-logo{width:82px;height:82px;border-radius:50%;object-fit:cover;border:3px solid #19a9ff;box-shadow:0 0 24px rgba(0,169,255,.45);position:relative;z-index:1;background:#06111d}
.vip-login-title{font-size:31px;font-weight:900;letter-spacing:1px;margin:12px 0 2px;background:linear-gradient(90deg,#ffd84d,#fff,#58c9ff);-webkit-background-clip:text;background-clip:text;color:transparent}
.vip-login-sub{font-size:17px;color:#2bbcff;font-weight:700;margin-bottom:7px}.vip-login-admin-title{font-size:36px!important;line-height:1.05!important;letter-spacing:.4px!important}.vip-login-secure{font-size:13px;color:#a9c8de;margin:0 0 22px}
.vip-login-field{display:flex;align-items:center;gap:11px;margin:12px 0;padding:0 14px;height:56px;border:1px solid rgba(0,164,255,.75);border-radius:15px;background:rgba(4,27,48,.78);box-shadow:inset 0 0 18px rgba(0,106,255,.07)}
.vip-login-field span{font-size:22px;opacity:.95}.vip-login-field input{width:100%;height:100%;border:0;outline:0;background:transparent;color:#eaf6ff;font-size:16px}.vip-login-field input::placeholder{color:#89a9c1}.vip-login-eye{cursor:pointer;background:none;border:0;color:#b9dcf5;font-size:20px;padding:5px}
.vip-login-row{display:flex;align-items:center;gap:9px;text-align:left;color:#d4e9f8;font-size:14px;margin:13px 2px 18px}.vip-login-row input{accent-color:#08a8ff;width:17px;height:17px}
#loginBtn{width:100%;height:56px;border:1px solid #31d0ff!important;border-radius:17px!important;background:linear-gradient(135deg,#0d5fe9,#117fe9 55%,#13a9ff)!important;color:#fff!important;font-size:18px!important;font-weight:900!important;letter-spacing:.6px;box-shadow:0 0 26px rgba(0,147,255,.38)!important;cursor:pointer}
#loginBtn:active{transform:translateY(1px)}#loginStatus{min-height:20px;margin-top:12px;font-size:13px}.vip-login-footer{margin-top:20px;color:#8fb0c8;font-size:13px}.vip-login-footer b{color:#1db8ff}
/* ===== DASHBOARD QUICK ACTIONS: equal-size 4 cards ===== */
.quick-grid{display:grid!important;grid-template-columns:repeat(4,minmax(0,1fr))!important;gap:14px!important;align-items:stretch!important}
.quick-grid>*{min-width:0!important;width:100%!important;box-sizing:border-box!important;display:flex!important;align-items:center!important;justify-content:center!important;min-height:68px!important}
.quick-grid .vip-import-wrap,.quick-grid .vip-import-menu,.quick-grid .import-file-wrap{width:100%!important;min-width:0!important}
/* ===== IMPORT PAGE: prevent squeezed left column ===== */
.vip-import-wrap{display:grid!important;grid-template-columns:minmax(240px,.85fr) minmax(0,2.15fr)!important;gap:22px!important;align-items:stretch!important;width:100%!important;box-sizing:border-box!important}
.vip-import-wrap>*{min-width:0!important;box-sizing:border-box!important}.vip-import-wrap textarea,.vip-import-wrap input,.vip-import-wrap select,.vip-import-wrap button{max-width:100%!important;box-sizing:border-box}
.vip-import-wrap .import-side,.vip-import-wrap .import-sidebar{min-width:0!important;width:auto!important}.vip-import-wrap .import-main,.vip-import-wrap .import-content{min-width:0!important;width:auto!important}
/* Import tabs stay compact and usable */
.import-tabs,.vip-import-tabs{display:flex!important;flex-wrap:wrap!important;gap:8px!important;margin-bottom:16px!important}.import-tab{flex:0 0 auto!important;min-width:0!important;white-space:nowrap!important}
/* Mobile/tablet */
@media(max-width:900px){
 .quick-grid{grid-template-columns:repeat(2,minmax(0,1fr))!important;gap:11px!important}.quick-grid>*{min-height:62px!important}
 .vip-import-wrap{grid-template-columns:1fr!important;gap:14px!important}.vip-import-wrap .import-side,.vip-import-wrap .import-sidebar{width:100%!important}.vip-import-wrap .import-main,.vip-import-wrap .import-content{width:100%!important}
}
@media(max-width:520px){
 .quick-grid{grid-template-columns:repeat(2,minmax(0,1fr))!important}.quick-grid>*{min-height:58px!important;font-size:13px!important}
 .vip-lux-login{padding:25px 18px 20px}.vip-login-title{font-size:25px}.vip-login-admin-title{font-size:29px!important}.vip-login-logo{width:72px;height:72px}
}
body.light .vip-lux-login{color:#10253a}.vip-lux-login *{box-sizing:border-box}
`;
  const s=document.createElement('style');s.id='vipLuxuryAdminStyle';s.textContent=css;document.head.appendChild(s);

  const modal=document.getElementById('loginModal');
  if(modal){
    const card=modal.querySelector('.login-card,.modal-card')||modal.firstElementChild;
    const host=card||modal;
    host.classList.add('vip-lux-login');
    host.innerHTML=`
      <img class="vip-login-logo" src="https://i.postimg.cc/fWC0JfBr/FB-IMG-1788617876279.jpg" alt="Admin Panel">
      <div class="vip-login-title">WELCOME</div>
      <div class="vip-login-sub vip-login-admin-title">Admin Panel</div>
      <div class="vip-login-secure">🛡️ Secure administrator access</div>
      <label class="vip-login-field"><span>🔒</span><input id="workerPassword" type="password" autocomplete="current-password" placeholder="Password" value="258085"><button class="vip-login-eye" type="button" id="vipLoginEye" aria-label="Show password">◉</button></label>
      <label class="vip-login-row"><input id="vipRemember" type="checkbox"> <span>Remember me</span></label>
      <button id="loginBtn" type="button">⇥ &nbsp; LOGIN</button>
      <div id="loginStatus"></div>
      <div class="vip-login-footer">ADMIN PANEL</div>`;
    document.getElementById('vipLoginEye')?.addEventListener('click',()=>{const p=document.getElementById('workerPassword');if(!p)return;p.type=p.type==='password'?'text':'password';});
  }
}

function saveLocal(){localStorage.setItem('vipChannels',JSON.stringify(channels));localStorage.setItem('vipCategories',JSON.stringify(categories));}
function toast(t){const x=document.getElementById('toast');if(!x)return;x.textContent=t;x.classList.add('show');setTimeout(()=>x.classList.remove('show'),2200)}
function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
function statusClass(s){return String(s||'Unknown').toLowerCase().replace(/\s*\/\s*/g,'-').replace(/\s+/g,'-')}
function logoHTML(c){return c.logo?`<img class="logo-cell" src="${esc(c.logo)}" onerror="this.style.display='none'">`:`<div class="logo-cell" style="display:grid;place-items:center;color:#08a9ff;font-weight:800">TV</div>`}
async function api(path,options={}){const headers=new Headers(options.headers||{});headers.set('Accept','application/json');if(options.body&&!(options.body instanceof FormData)&&!headers.has('Content-Type'))headers.set('Content-Type','application/json');const r=await fetch(WORKER_API.replace(/\/$/,'')+path,{...options,headers,credentials:'include',cache:'no-store'});const text=await r.text();let data={};try{data=text?JSON.parse(text):{}}catch{data={raw:text}}if(!r.ok)throw new Error(data?.error||data?.message||('HTTP '+r.status));return data;}
function showLogin(show=true){document.getElementById('loginModal')?.classList.toggle('show',show)}
function setLoginStatus(t,bad=false){const x=document.getElementById('loginStatus');if(x){x.textContent=t;x.style.color=bad?'#ff7070':''}}
async function loginWorker(){const workerInput=document.getElementById('workerUrl'),url=(workerInput?workerInput.value.trim():DEFAULT_WORKER_API).replace(/\/$/,''),pw=document.getElementById('workerPassword')?.value||'';if(!url||!pw)return setLoginStatus('Enter Admin Password.',true);WORKER_API=url;setLoginStatus('Connecting…');try{await api('/api/admin/login',{method:'POST',body:JSON.stringify({password:pw})});connected=true;showLogin(false);setBackendState(true);setLoginStatus('');const pf=document.getElementById('workerPassword');if(pf)pf.value='';await loadRemoteState();await loadDevices();toast('Connected • session valid for 5 minutes')}catch(e){connected=false;setBackendState(false);setLoginStatus('Login failed: '+e.message,true)}}
document.getElementById('logi
async function logoutWorker(){try{await api('/api/admin/logout',{method:'POST'})}catch{}connected=false;setBackendState(false);showLogin(true);document.getElementById('workerPassword').value='';toast('Logged out')}
function setBackendState(ok){document.querySelectorAll('.online').forEach(x=>x.innerHTML=ok?'<i></i> Online':'<i style="background:#ff5d73"></i> Offline');document.querySelectorAll('.status-bar b').forEach(x=>x.innerHTML=`Backend connection: <i style="color:${ok?'#43e59a':'#ff5d73'}">${ok?'Connected':'Offline'}</i>`)}
function showSection(id,fromHistory=false){document.querySelectorAll('.section').forEach(x=>x.classList.remove('active-section'));const el=document.getElementById(id);if(el)el.classList.add('active-section');document.querySelectorAll('.nav-item').forEach(x=>x.classList.toggle('active',x.dataset.section===id));document.getElementById('sidebar')?.classList.remove('open');render();if(id==='devices'&&connected)loadDevices();if(!fromHistory){history.pushState({adminSection:id},'',`#${encodeURIComponent(id)}`)}}
window.addEventListener('popstate',()=>{const id=decodeURIComponent(location.hash.slice(1)||'dashboard');showSection(document.getElementById(id)?id:'dashboard',true)});
document.querySelectorAll('.nav-item').forEach(b=>b.addEventListener('click',()=>showSection(b.dataset.section)));document.querySelectorAll('.import-tab').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.import-tab').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.import-pane').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.getElementById('import-'+b.dataset.importTab)?.classList.add('active')}));document.getElementById('menuBtn')?.addEventListener('click',()=>document.getElementById('sidebar')?.classList.toggle('open'));document.getElementById('themeBtn')?.addEventListener('click',()=>{document.body.classList.toggle('light');toast('Theme toggled')});document.getElementById('profileBtn')?.addEventListener('click',()=>toast(connected?'ADMIN • Connected':'ADMIN • Not connected'));
function normalizeChannels(x){if(!Array.isArray(x))return [];return x.map(c=>({name:c.name||c.title||'Unnamed',category:c.category||c.group||c.groupTitle||'Other',logo:c.logo||c.tvgLogo||'',url:c.url||c.stream||c.streamUrl||'',status:c.status||'Unknown',...c})).filter(c=>c.url||c.name)}
function extractState(data){const s=data?.state&&typeof data.state==='object'?data.state:data;const list=s?.channels||s?.playlist?.channels||data?.channels||data?.playlist||[];return{channels:normalizeChannels(list),notice:s?.notice||data?.notice||{},headline:s?.headline||data?.headline||''}}
async function loadRemoteState(){if(!connected)return;try{const data=await api('/api/admin/state');const s=extractState(data);channels=s.channels;if(!channels.length){const p=await api('/api/admin/playlist');channels=normalizeChannels(p?.channels||p?.playlist||p)}categories=[...new Set([...(Array.isArray(s.categories)?s.categories:[]),'News','Entertainment','Sports','Kids','Movies','Music','Other',...channels.map(c=>c.category).filter(Boolean)])];saveLocal();render();fillNotice(s.notice,s.headline);document.getElementById('syncTime').textContent=new Date().toLocaleString()}catch(e){if(String(e.message).includes('Unauthorized')){connected=false;showLogin(true);setLoginStatus('Session expired. Please login again.',true)}toast('Could not load Worker state: '+e.message);setBackendState(false)}}
async function saveRemoteState(extra={}){if(!connected){saveLocal();return true}try{await api('/api/admin/state',{method:'PUT',body:JSON.stringify({channels,categories,...extra})});saveLocal();document.getElementById('syncTime').textContent=new Date().toLocaleString();return true}catch(e){toast('Worker save failed: '+e.message);return false}}
function filtered(list,searchId,catId,statusId){let s=(document.getElementById(searchId)?.value||'').toLowerCase(),cat=document.getElementById(catId)?.value||'All Categories',st=document.getElementById(statusId)?.value||'All Status';return list.filter(c=>(!s||String(c.name).toLowerCase().includes(s))&&(cat==='All Categories'||c.category===cat)&&(st==='All Status'||c.status===st))}
function rows(list,full=false){return list.map((c,i)=>{const idx=channels.indexOf(c);return `<tr><td>${i+1}</td><td>${logoHTML(c)}</td><td><b>${esc(c.name)}</b></td><td>${esc(c.category)}</td><td><span class="badge ${statusClass(c.status)}">● ${esc(c.status)}</span></td>${full?`<td title="${esc(c.url)}">${esc(c.url).slice(0,42)}${String(c.url).length>42?'…':''}</td>`:''}<td><div class="actions"><button onclick="preview(${idx})">▶</button><button onclick="editChannel(${idx})">✎</button><button class="del" onclick="deleteChannel(${idx})">▣</button></div></td></tr>`}).join('')}
function renderPagination(total,page,setter){const pages=Math.max(1,Math.ceil(total/PAGE_SIZE));page=Math.min(page,pages);let out=`<button ${page<=1?'disabled':''} onclick="${setter}(${page-1})">«</button>`;for(let p=1;p<=pages;p++)out+=`<button class="${p===page?'active':''}" onclick="${setter}(${p})">${p}</button>`;return out+`<button ${page>=pages?'disabled':''} onclick="${setter}(${page+1})">»</button>`}
function setDashPage(p){dashPage=Math.max(1,p);render()}function setManagerPage(p){managerPage=Math.max(1,p);render()}
function render(){const total=channels.length;document.getElementById('totalStat').textContent=total;document.getElementById('activeStat').textContent=channels.filter(c=>c.status==='Active').length;document.getElementById('deadStat').textContent=channels.filter(c=>c.status==='Dead').length;document.getElementById('unknownStat').textContent=channels.filter(c=>c.status!=='Active'&&c.status!=='Dead').length;const opts='<option>All Categories</option>'+categories.map(c=>`<option>${esc(c)}</option>`).join('');['dashCat','managerCat'].forEach(id=>{const el=document.getElementById(id);if(el){const old=el.value;el.innerHTML=opts;if([...el.options].some(o=>o.value===old))el.value=old}});const ac=document.getElementById('addCategory');if(ac)ac.innerHTML=categories.map(c=>`<option>${esc(c)}</option>`).join('');const d=filtered(channels,'dashSearch','dashCat','dashStatus'),m=filtered(channels,'managerSearch','managerCat','managerStatus');dashPage=Math.min(dashPage,Math.max(1,Math.ceil(d.length/PAGE_SIZE)));managerPage=Math.min(managerPage,Math.max(1,Math.ceil(m.length/PAGE_SIZE)));document.getElementById('channelRows').innerHTML=rows(d.slice((dashPage-1)*PAGE_SIZE,dashPage*PAGE_SIZE));document.getElementById('managerRows').innerHTML=rows(m.slice((managerPage-1)*PAGE_SIZE,managerPage*PAGE_SIZE),true);document.getElementById('pagination').innerHTML=d.length?renderPagination(d.length,dashPage,'setDashPage'):'<span>No channels found</span>';document.getElementById('categoryList').innerHTML=categories.map((c,i)=>`<span>${esc(c)} <button onclick="removeCategory(${i})">×</button></span>`).join('');const base=Math.max(total,1);document.getElementById('barActive').style.width=(channels.filter(c=>c.status==='Active').length/base*100)+'%';document.getElementById('barDead').style.width=(channels.filter(c=>c.status==='Dead').length/base*100)+'%';document.getElementById('barDisabled').style.width=(channels.filter(c=>c.status!=='Active'&&c.status!=='Dead').length/base*100)+'%';if(document.getElementById('syncTime')&&!document.getElementById('syncTime').textContent)document.getElementById('syncTime').textContent=new Date().toLocaleString()}
['dashSearch','dashCat','dashStatus','managerSearch','managerCat','managerStatus'].forEach(id=>document.getElementById(id)?.addEventListener('input',()=>{dashPage=1;managerPage=1;render()}));async function refreshData(){if(connected)await loadRemoteState();else render();toast(connected?'Worker data refreshed':'Local data refreshed')}
function preview(i){selected=i;const c=channels[i];if(!c)return;document.getElementById('pName').textContent=c.name;document.getElementById('pCategory').textContent=c.category;document.getElementById('pUrl').textContent=c.url;document.getElementById('pStatus').textContent=c.status;document.getElementById('testUrl').value=c.url;const v=document.getElementById('player');v.src=c.url;document.getElementById('videoPlaceholder').style.display='none';document.querySelector('.live-dot').textContent='● '+c.name;toast(c.name+' selected')}
document.getElementById('playBtn')?.addEventListener('click',()=>{if(selected!==null)document.getElementById('player')?.play().catch(()=>toast('Browser blocked playback; check stream URL'))});document.getElementById('reloadBtn')?.addEventListener('click',()=>{const v=document.getElementById('player');if(v)v.load();toast('Player reloaded')});document.getElementById('fullBtn')?.addEventListener('click',()=>document.getElementById('player')?.requestFullscreen?.());document.getElementById('openBtn')?.addEventListener('click',()=>{if(selected!==null)window.open(channels[selected].url,'_blank')});
async function deleteChannel(i){if(!channels[i]||!confirm('Delete '+channels[i].name+'?'))return;channels.splice(i,1);if(await saveRemoteState()){render();toast('Channel deleted')}}async function editChannel(i){const c=channels[i];if(!c)return;const name=prompt('Channel name:',c.name);if(name===null)return;const url=prompt('Stream URL:',c.url);if(url===null)return;const cat=prompt('Category:',c.category)||c.category;c.name=name.trim()||c.name;c.url=url.trim();c.category=cat;if(!categories.includes(cat))categories.push(cat);if(await saveRemoteState()){render();toast('Channel updated')}}
document.getElementById('channelForm')?.addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.target),c={name:f.get('name'),category:f.get('category'),logo:f.get('logo'),url:f.get('url'),status:f.get('status')};channels.push(c);if(!categories.includes(c.category))categories.push(c.category);if(await saveRemoteState()){e.target.reset();render();showSection('channels');toast('Channel added successfully')}});
function parseM3U(text){const lines=text.split(/\r?\n/).map(x=>x.trim()).filter(Boolean),out=[];for(let i=0;i<lines.length;i++){if(lines[i].startsWith('#EXTINF')){const info=lines[i],url=lines[i+1]||'';if(!url.startsWith('#')){const comma=info.indexOf(','),name=comma>=0?info.slice(comma+1).trim():'Unknown',logo=(info.match(/tvg-logo="([^"]*)"/i)||[])[1]||'',cat=(info.match(/group-title="([^"]*)"/i)||[])[1]||'Other';out.push({name,category:cat,logo,url,status:'Active'});i++}}}return out}
async function addImported(list,replace=false){if(!list.length)return toast('No valid channels found');if(replace)channels=list;else channels.push(...list);list.forEach(c=>{if(c.category&&!categories.includes(c.category))categories.push(c.category)});if(await saveRemoteState()){render();showSection('channels');toast(list.length+' channels imported')}}async function importM3U(){await addImported(parseM3U(document.getElementById('m3uText').value),true);if(document.getElementById('m3uText'))document.getElementById('m3uText').value=''}
async function importM3UUrl(){const u=document.getElementById('m3uUrl').value.trim();if(!u)return toast('Enter an M3U URL');if(connected){try{toast('Worker is importing M3U…');const data=await api('/api/admin/import-m3u-url',{method:'POST',body:JSON.stringify({url:u})});await loadRemoteState();toast(data?.count?data.count+' channels imported':'M3U imported');document.getElementById('m3uUrl').value=''}catch(e){toast('Worker M3U import failed: '+e.message)}}else{try{toast('Loading M3U URL…');const r=await fetch(u,{cache:'no-store'});if(!r.ok)throw new Error('HTTP '+r.status);await addImported(parseM3U(await r.text()),true)}catch(e){toast('Could not load URL. Check URL/CORS.')}}}
async function importXtream(){const server=document.getElementById('xtServer').value.trim().replace(/\/$/,''),user=document.getElementById('xtUser').value.trim(),pass=document.getElementById('xtPass').value,lim=document.getElementById('xtLimit').value;if(!server||!user||!pass)return toast('Enter server, username and password');if(connected){try{toast('Worker is importing Xtream…');const data=await api('/api/xtream/import',{method:'POST',body:JSON.stringify({server,username:user,password:pass,limit:lim})});await loadRemoteState();toast(data?.count?data.count+' channels imported':'Xtream imported')}catch(e){toast('Xtream import failed: '+e.message)}}else{try{const apiurl=server+'/player_api.php?username='+encodeURIComponent(user)+'&password='+encodeURIComponent(pass)+'&action=get_live_streams',r=await fetch(apiurl,{cache:'no-store'});if(!r.ok)throw new Error('HTTP '+r.status);const data=await r.json(),items=lim==='all'?data:data.slice(0,Number(lim)),base=server+'/live/'+encodeURIComponent(user)+'/'+encodeURIComponent(pass)+'/';await addImported(items.map(x=>({name:x.name||('Channel '+x.stream_id),category:x.category_name||'Other',logo:x.stream_icon||'',url:base+encodeURIComponent(String(x.stream_id))+'.m3u8',status:'Active'})),true)}catch(e){toast('Xtream import failed. Check credentials/server/CORS.')}}}
document.getElementById('m3uFile')?.addEventListener('change',e=>{const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=()=>document.getElementById('m3uText').value=r.result;r.readAsText(f)});async function addCategory(){const n=document.getElementById('newCat').value.trim();if(!n)return;if(categories.includes(n))return toast('Category already exists');categories.push(n);if(await saveRemoteState()){render();document.getElementById('newCat').value='';toast('Category added')}}async function removeCategory(i){const n=categories[i];if(channels.some(c=>c.category===n))return toast('Category is in use');categories.splice(i,1);saveLocal();render();toast('Category removed')}
function download(name,text,type){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type}));a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}function exportData(){download('vip-network-backup.json',JSON.stringify({channels,categories,exported:new Date().toISOString()},null,2),'application/json');toast('JSON backup exported')}function exportM3U(){const t='#EXTM3U\n'+channels.map(c=>`#EXTINF:-1 tvg-logo="${c.logo||''}" group-title="${c.category}",${c.name}\n${c.url}`).join('\n');download('vip-network-playlist.m3u',t,'audio/x-mpegurl');toast('M3U exported')}
document.getElementById('restoreFile')?.addEventListener('change',e=>{const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=async()=>{try{const x=JSON.parse(r.result);if(Array.isArray(x.channels)){channels=x.channels;categories=x.categories||categories;if(await saveRemoteState()){render();toast('Backup restored')}}}catch{toast('Invalid JSON backup')}};r.readAsText(f)});async function clearAll(){if(!confirm('Clear all channels?'))return;channels=[];if(await saveRemoteState()){render();toast('All channels cleared')}}function testStream(){const u=document.getElementById('testUrl').value.trim();if(!u)return toast('Enter a stream URL');const v=document.getElementById('player');v.src=u;document.getElementById('videoPlaceholder').style.display='none';v.play().catch(()=>toast('URL loaded. Browser may not support this stream format.'))}
document.getElementById('globalSearch')?.addEventListener('input',e=>{const q=e.target.value.trim();if(q){showSection('channels');document.getElementById('managerSearch').value=q;render()}});function fillNotice(notice={},headline=''){const f=document.getElementById('noticeForm');if(!f)return;f.elements.text.value=notice.text||headline||'';f.elements.type.value=notice.type||'Information';f.elements.enabled.value=(notice.enabled===false||notice.enabled==='No')?'No':'Yes'}
document.getElementById('noticeForm')?.addEventListener('submit',async e=>{e.preventDefault();const x=Object.fromEntries(new FormData(e.target));const notice={...x,enabled:x.enabled==='Yes'};if(connected){try{await saveRemoteState({notice,headline:x.text});toast('Banner saved to VIP NETWORK')}catch(e){toast('Banner save failed: '+e.message)}}else{localStorage.setItem('vipNotice',JSON.stringify(notice));toast('Banner saved locally')}});document.getElementById('settingsForm')?.addEventListener('submit',async e=>{e.preventDefault();const settings=Object.fromEntries(new FormData(e.target));if(connected){try{await api('/api/admin/settings',{method:'PUT',body:JSON.stringify(settings)});toast('Website settings saved to Worker')}catch(e){toast('Settings save failed: '+e.message)}}else{localStorage.setItem('vipSettings',JSON.stringify(settings));toast('Settings saved locally')}});
function formatLoginTime(v){if(!v)return 'Never';try{return new Date(v).toLocaleString()}catch{return String(v)}}
async function loadDevices(){if(!connected)return;const box=document.getElementById('deviceList');if(!box)return;box.innerHTML='<div>Loading users…</div>';try{const d=await api('/api/admin/devices'),list=Array.isArray(d)?d:(d.devices||d.data||[]);const c=document.getElementById('deviceConnected');if(c)c.textContent=list.length;const limit=d?.settings?.deviceLimit??d?.settings?.maxDevices??d?.deviceLimit??'—';const a=document.getElementById('deviceAllowed');if(a)a.textContent=limit;box.innerHTML=list.length?list.map(x=>{const id=esc(x.deviceId||x.id||''),blocked=x.status==='Blocked'||x.blocked===true;const name=esc(x.userName||x.username||x.name||x.deviceName||x.deviceId||'User');const dev=esc(x.name||x.deviceName||'Device');const status=blocked?'Blocked':esc(x.status||'Logged in');return `<div><b>${name}</b> <span>${dev}</span><br><small>Last login: ${esc(formatLoginTime(x.lastLogin||x.lastSeen||x.createdAt))}</small> <b>${status}</b><div class="actions">${blocked?`<button onclick="unblockDevice('${id}')">↗</button>`:`<button onclick="blockDevice('${id}')">⛔</button>`}<button class="del" onclick="deleteDevice('${id}')">▣</button></div></div>`}).join(''):'<div>No users registered.</div>'}catch(e){box.innerHTML='<div>Could not load users: '+esc(e.message)+'</div>'}}
async function approveDevice(id){if(!id)return;try{await api('/api/admin/devices/approve',{method:'POST',body:JSON.stringify({deviceId:id})});await loadDevices();toast('User approved')}catch(e){toast(e.message)}}async function approveAllDevices(){try{await api('/api/admin/devices/approve-all',{method:'POST',body:'{}'});await loadDevices();toast('All users approved')}catch(e){toast(e.message)}}async function blockDevice(id){if(!id||!confirm('Block this user? They will not be able to enter the TV website.'))return;try{await api('/api/admin/devices/block',{method:'POST',body:JSON.stringify({deviceId:id})});await loadDevices();toast('User blocked')}catch(e){toast(e.message)}}async function unblockDevice(id){if(!id)return;try{await api('/api/admin/devices/unblock',{method:'POST',body:JSON.stringify({deviceId:id})});await loadDevices();toast('User unblocked')}catch(e){toast(e.message)}}async function logoutAllDevices(){if(!confirm('Log out all devices?'))return;try{await api('/api/admin/devices/logout-all',{method:'POST',body:'{}'});await loadDevices();toast('All users logged out')}catch(e){toast(e.message)}}async function deleteDevice(id){if(!id||!confirm('Delete this user/device?'))return;try{await api('/api/admin/devices?deviceId='+encodeURIComponent(id),{method:'DELETE'});await loadDevices();toast('User deleted')}catch(e){toast(e.message)}}
injectLuxuryAdminUI();
Object.assign(window,{loginWorker,logoutWorker,showSection,refreshData,preview,editChannel,deleteChannel,setDashPage,setManagerPage,importM3U,importM3UUrl,importXtream,addCategory,removeCategory,exportData,exportM3U,clearAll,testStream,approveDevice,approveAllDevices,blockDevice,unblockDevice,logoutAllDevices,deleteDevice});
(async function init(){setBackendState(false);render();const input=document.getElementById('workerUrl');if(input)input.value=DEFAULT_WORKER_API;const local=JSON.parse(localStorage.getItem('vipChannels')||'null');if(Array.isArray(local))channels=local;render();if(location.hash){showSection(decodeURIComponent(location.hash.slice(1)),true)}else showSection('dashboard',true);try{await api('/api/admin/session');connected=true;showLogin(false);setBackendState(true);await loadRemoteState();await loadDevices()}catch{showLogin(true)}})();
