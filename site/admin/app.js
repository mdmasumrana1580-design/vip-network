const API=(window.VIP_ADMIN_WORKER_API||window.location.origin).replace(/\/$/,'');
let connected=false,channels=[],tvChannels=[],movieSeries=[],categories=['SPORTS','BD','INDIA','OTHERS','MOVIE & SERIES'],selected=-1;

function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}
function toast(t){const x=document.getElementById('toast');x.textContent=t;x.classList.add('show');setTimeout(()=>x.classList.remove('show'),2400)}
function logAction(text){const a=JSON.parse(localStorage.getItem('vipActivity')||'[]');a.unshift({text,time:new Date().toLocaleString()});localStorage.setItem('vipActivity',JSON.stringify(a.slice(0,100)));renderLogs()}
function renderLogs(){const box=document.getElementById('activityList');if(!box)return;const a=JSON.parse(localStorage.getItem('vipActivity')||'[]');box.innerHTML=a.length?a.map(x=>`<div><b>${esc(x.text)}</b><small>${esc(x.time)}</small></div>`).join(''):'No activity yet.'}
function clearLogs(){localStorage.removeItem('vipActivity');renderLogs();toast('Activity logs cleared')}
async function api(path,opt={}){const h=new Headers(opt.headers||{});h.set('Accept','application/json');if(opt.body&&!h.has('Content-Type'))h.set('Content-Type','application/json');const r=await fetch(API+path,{...opt,headers:h,credentials:'include',cache:'no-store'});const text=await r.text();let d={};try{d=text?JSON.parse(text):{}}catch{d={raw:text}}if(!r.ok)throw new Error(d.error||('HTTP '+r.status));return d}
function setBackend(ok){connected=ok;const e=document.getElementById('backendState');if(e)e.textContent=ok?'Connected':'Offline';document.querySelectorAll('.online').forEach(x=>x.innerHTML=ok?'<i></i> Online':'Offline')}
function showLogin(show=true){document.getElementById('loginModal').classList.toggle('show',show)}
function loginStatus(t,bad=false){const x=document.getElementById('loginStatus');x.textContent=t;x.style.color=bad?'#ff7b8d':'#76d7ff'}
async function loginWorker(){const p=document.getElementById('workerPassword').value;if(!p)return loginStatus('Enter Admin Password.',true);const b=document.getElementById('loginBtn');b.disabled=true;loginStatus('Connecting...');try{await api('/api/admin/login',{method:'POST',body:JSON.stringify({password:p})});setBackend(true);showLogin(false);document.getElementById('workerPassword').value='';await loadRemoteState();await loadDevices();logAction('Admin logged in');toast('Connected')}catch(e){loginStatus('Login failed: '+e.message,true)}finally{b.disabled=false}}
async function logoutWorker(){try{await api('/api/admin/logout',{method:'POST'})}catch{}setBackend(false);showLogin(true);logAction('Admin logged out')}
function norm(c){return {name:c.name||c.title||'Unnamed',category:c.category||c.group||'OTHERS',logo:c.logo||c.tvgLogo||'',url:c.url||c.stream||'',status:c.status||'Unknown'}}
function rebuildChannels(){channels=[...tvChannels,...movieSeries]}
async function loadRemoteState(){const d=await api('/api/admin/state');const s=d.state||d;tvChannels=Array.isArray(s.tvChannels)?s.tvChannels.map(norm):(Array.isArray(s.channels)?s.channels.filter(c=>String(c.category||'').toUpperCase()!=='MOVIE & SERIES').map(norm):[]);movieSeries=Array.isArray(s.movieSeries)?s.movieSeries.map(c=>({...norm(c),category:'MOVIE & SERIES'})):(Array.isArray(s.channels)?s.channels.filter(c=>String(c.category||'').toUpperCase()==='MOVIE & SERIES').map(c=>({...norm(c),category:'MOVIE & SERIES'})):[]);rebuildChannels();categories=[...new Set([...categories,...channels.map(x=>x.category).filter(Boolean)])];saveLocal();render();const t=document.getElementById('syncTime');if(t)t.textContent=new Date().toLocaleString()}
function saveLocal(){localStorage.setItem('vipChannels',JSON.stringify(channels));localStorage.setItem('vipTvChannels',JSON.stringify(tvChannels));localStorage.setItem('vipMovieSeries',JSON.stringify(movieSeries));localStorage.setItem('vipCategories',JSON.stringify(categories))}
async function saveRemoteState(extra={}){try{const payload={channels,tvChannels:channels.filter(x=>x.category!=='MOVIE & SERIES'),movieSeries:channels.filter(x=>x.category==='MOVIE & SERIES').map(x=>({...x,category:'MOVIE & SERIES'})),...extra};await api('/api/admin/state',{method:'PUT',body:JSON.stringify(payload)});tvChannels=payload.tvChannels.map(norm);movieSeries=payload.movieSeries.map(x=>({...norm(x),category:'MOVIE & SERIES'}));rebuildChannels();saveLocal();return true}catch(e){toast('Save failed: '+e.message);return false}}
function logo(c){return c.logo?`<img class="logo-cell" src="${esc(c.logo)}" onerror="this.style.display='none'">`:'<span class="logo-cell"></span>'}
function rows(list,full=false){return list.map((c,i)=>{const idx=channels.indexOf(c);return `<tr><td>${i+1}</td><td>${logo(c)}</td><td><b>${esc(c.name)}</b></td><td>${esc(c.category)}</td><td><span class="badge ${String(c.status).toLowerCase()}">● ${esc(c.status)}</span></td>${full?`<td>${esc(c.url)}</td>`:''}<td><div class="actions"><button onclick="preview(${idx})">▶</button><button onclick="editChannel(${idx})">✎</button><button class="del" onclick="deleteChannel(${idx})">⌫</button></div></td></tr>`}).join('')}
function filter(list,sid,cid,stid){const s=(document.getElementById(sid)?.value||'').toLowerCase(),c=document.getElementById(cid)?.value||'All Categories',st=document.getElementById(stid)?.value||'All Status';return list.filter(x=>(!s||x.name.toLowerCase().includes(s))&&(c==='All Categories'||x.category===c)&&(st==='All Status'||x.status===st))}
function fillSelect(id){const e=document.getElementById(id);if(!e)return;const old=e.value;e.innerHTML='<option>All Categories</option>'+categories.map(x=>`<option>${esc(x)}</option>`).join('');if([...e.options].some(o=>o.value===old))e.value=old}
function render(){['dashCat','managerCat'].forEach(fillSelect);const add=document.getElementById('addCategory');if(add)add.innerHTML=categories.map(x=>`<option>${esc(x)}</option>`).join('');document.getElementById('totalStat').textContent=channels.length;document.getElementById('activeStat').textContent=channels.filter(x=>x.status==='Active').length;document.getElementById('deadStat').textContent=channels.filter(x=>x.status==='Dead').length;document.getElementById('categoryStat').textContent=categories.length;const d=filter(channels,'dashSearch','dashCat',null),m=filter(channels,'managerSearch','managerCat','managerStatus');document.getElementById('channelRows').innerHTML=rows(d);document.getElementById('managerRows').innerHTML=rows(m,true);const cl=document.getElementById('categoryList');if(cl)cl.innerHTML=categories.map((x,i)=>`<span>${esc(x)} <button onclick="removeCategory(${i})">×</button></span>`).join('')}
function fillEditPanel(c){if(!c)return;const n=document.getElementById('editName'),cat=document.getElementById('editCategory'),url=document.getElementById('editUrl'),st=document.getElementById('editStatus'),logo=document.getElementById('editLogo'),file=document.getElementById('editLogoFile'),img=document.getElementById('editLogoPreview'),msg=document.getElementById('editLogoStatus');if(n)n.value=c.name||'';if(cat){cat.innerHTML=categories.map(x=>`<option>${esc(x)}</option>`).join('');cat.value=c.category||categories[0]||'OTHERS'}if(url)url.value=c.url||'';if(st)st.value=c.status||'Unknown';if(logo)logo.value=c.logo&&/^https?:\/\//i.test(c.logo)?c.logo:'';if(file)file.value='';if(img){if(c.logo){img.src=c.logo;img.style.display='block';}else{img.removeAttribute('src');img.style.display='none'}}if(msg)msg.textContent=c.logo?'Logo loaded':'No logo selected'}
function preview(i){selected=i;const c=channels[i];if(!c)return;document.getElementById('pName').textContent=c.name;document.getElementById('pCategory').textContent=c.category;document.getElementById('pStatus').textContent=c.status;document.getElementById('testUrl').value=c.url;const v=document.getElementById('player');v.src=c.url;document.getElementById('videoPlaceholder').style.display='none';fillEditPanel(c)}
async function saveSelectedChannel(){
  if(selected<0||!channels[selected])return toast('Select a channel first');
  const c=channels[selected];
  const oldCategory=c.category||'OTHERS';
  const name=document.getElementById('editName').value.trim();
  let cat=document.getElementById('editCategory').value.trim()||oldCategory;
  const url=document.getElementById('editUrl').value.trim();
  const status=document.getElementById('editStatus').value;
  const logoUrl=document.getElementById('editLogo').value.trim();
  const file=document.getElementById('editLogoFile').files[0];
  if(!name)return toast('Channel name is required');
  cat=cat.toUpperCase()==='OTHER'?'OTHERS':cat;
  const wasMovie=String(oldCategory).toUpperCase()==='MOVIE & SERIES';
  const isMovie=String(cat).toUpperCase()==='MOVIE & SERIES';
  c.name=name;c.category=cat;c.url=url;c.status=status;
  if(!file&&logoUrl)c.logo=logoUrl;
  if(!file&&!logoUrl)c.logo='';
  if(file){try{c.logo=await readLogoFile(file)}catch(e){return toast('Logo file could not be read')}}
  if(!categories.includes(cat))categories.push(cat);
  if(wasMovie&&!isMovie){const p=movieSeries.indexOf(c);if(p>=0)movieSeries.splice(p,1);if(!tvChannels.includes(c))tvChannels.push(c)}
  else if(!wasMovie&&isMovie){const p=tvChannels.indexOf(c);if(p>=0)tvChannels.splice(p,1);if(!movieSeries.includes(c))movieSeries.push(c);c.category='MOVIE & SERIES'}
  else if(isMovie){c.category='MOVIE & SERIES'}
  rebuildChannels();
  selected=channels.indexOf(c);
  if(await saveRemoteState()){render();preview(selected);logAction('Edited channel: '+c.name);toast('Channel updated')}}
function readLogoFile(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>{const img=new Image();img.onload=()=>{const max=320,scale=Math.min(1,max/img.width,max/img.height),w=Math.max(1,Math.round(img.width*scale)),h=Math.max(1,Math.round(img.height*scale));const cv=document.createElement('canvas');cv.width=w;cv.height=h;const ctx=cv.getContext('2d');ctx.drawImage(img,0,0,w,h);resolve(cv.toDataURL('image/webp',0.86))};img.onerror=reject;img.src=r.result};r.onerror=reject;r.readAsDataURL(file)})}
async function deleteChannel(i){if(!channels[i]||!confirm('Delete this channel?'))return;const target=channels[i],name=target.name;const arr=target.category==='MOVIE & SERIES'?movieSeries:tvChannels;const pos=arr.indexOf(target);if(pos>=0)arr.splice(pos,1);rebuildChannels();if(await saveRemoteState()){render();logAction('Deleted channel: '+name);toast('Channel deleted')}}
async function editChannel(i){const c=channels[i];if(!c)return;const name=prompt('Channel name:',c.name);if(name===null)return;const url=prompt('Stream URL:',c.url);if(url===null)return;const cat=prompt('Category:',c.category)||c.category;c.name=name.trim()||c.name;c.url=url.trim();c.category=cat;if(!categories.includes(cat))categories.push(cat);if(await saveRemoteState()){render();logAction('Edited channel: '+c.name);toast('Channel updated')}}
function parseM3U(text,forceMovie=false){const l=String(text||'').replace(/\r/g,'').split('\n'),out=[];let meta=null;for(const raw of l){const x=raw.trim();if(x.startsWith('#EXTINF')){const comma=x.indexOf(','),name=comma>=0?x.slice(comma+1).trim():'Channel',logo=(x.match(/tvg-logo="([^"]*)"/i)||[])[1]||'',group=(x.match(/group-title="([^"]*)"/i)||[])[1]||'';meta={name,logo,group};continue}if(x&&!x.startsWith('#')&&meta){let category=forceMovie?'MOVIE & SERIES':(meta.group||'');if(!forceMovie){category=category?category.toUpperCase():'OTHER';if(['SPORTS','BD','INDIA','OTHER','OTHERS','MOVIE & SERIES'].includes(category)){if(category==='OTHERS')category='OTHER'}else{const low=category.toLowerCase();if(/india|indian/.test(low))category='INDIA';else if(/sport|cricket|football/.test(low))category='SPORTS';else if(/bangla|bangladesh|\bbd\b/.test(low))category='BD';else category='OTHER'}}out.push({name:meta.name,logo:meta.logo,category,url:x,status:'Active'});meta=null}}return out}
async function importTvM3U(){const list=parseM3U(document.getElementById('tvM3uText').value,false);if(!list.length)return toast('No valid Live TV channels found');tvChannels=list;rebuildChannels();categories=[...new Set([...categories,...list.map(x=>x.category)])];if(await saveRemoteState()){render();logAction('Updated Live TV playlist: '+list.length+' channels');toast(list.length+' Live TV channels imported')}}
async function importTvM3UUrl(){const url=document.getElementById('tvM3uUrl').value.trim();if(!url)return toast('Enter Live TV M3U URL');try{const d=await api('/api/admin/import-tv-m3u-url',{method:'POST',body:JSON.stringify({url})});await loadRemoteState();logAction('Updated Live TV playlist from URL');toast((d.count||0)+' Live TV channels imported')}catch(e){toast('Live TV import failed: '+e.message)}}
async function importTvXtream(){const server=document.getElementById('tvXtServer').value.trim(),username=document.getElementById('tvXtUser').value,password=document.getElementById('tvXtPass').value,limit=document.getElementById('tvXtLimit').value;if(!server||!username||!password)return toast('Enter server, username and password');try{const d=await api('/api/xtream/import',{method:'POST',body:JSON.stringify({server,username,password,limit})});await loadRemoteState();logAction('Updated Live TV from Xtream');toast((d.count||0)+' Live TV channels imported')}catch(e){toast('Live TV Xtream import failed: '+e.message)}}
async function importMovieM3U(){const list=parseM3U(document.getElementById('movieM3uText').value,true);if(!list.length)return toast('No valid Movie & Series items found');movieSeries=list;rebuildChannels();categories=[...new Set([...categories,'MOVIE & SERIES'])];if(await saveRemoteState()){render();logAction('Updated Movie & Series playlist: '+list.length+' items');toast(list.length+' Movie & Series items imported')}}
async function importMovieM3UUrl(){const url=document.getElementById('movieM3uUrl').value.trim();if(!url)return toast('Enter Movie & Series M3U URL');try{const d=await api('/api/admin/import-movie-m3u-url',{method:'POST',body:JSON.stringify({url})});await loadRemoteState();logAction('Updated Movie & Series playlist from URL');toast((d.count||0)+' Movie & Series items imported')}catch(e){toast('Movie & Series import failed: '+e.message)}}
// Backward-compatible aliases for any existing buttons or integrations.
async function importM3U(){return importTvM3U()}
async function importM3UUrl(){return importTvM3UUrl()}
async function importXtream(){return importTvXtream()}
async function addCategory(){const e=document.getElementById('newCat'),n=e.value.trim();if(!n)return;if(categories.includes(n))return toast('Already exists');categories.push(n);saveLocal();render();e.value='';logAction('Added category: '+n)}
function removeCategory(i){const n=categories[i];if(channels.some(x=>x.category===n))return toast('Category is in use');categories.splice(i,1);saveLocal();render();logAction('Removed category: '+n)}
function download(name,text,type){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type}));a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
function exportData(){download('vip-network-backup.json',JSON.stringify({channels,categories},null,2),'application/json');logAction('Exported JSON backup')}
function exportM3U(){download('vip-network-playlist.m3u','#EXTM3U\n'+channels.map(c=>`#EXTINF:-1 tvg-logo="${c.logo}" group-title="${c.category}",${c.name}\n${c.url}`).join('\n'),'audio/x-mpegurl');logAction('Exported M3U playlist')}
async function clearAll(){if(!confirm('Clear all channels?'))return;channels=[];if(await saveRemoteState()){render();logAction('Cleared all channels')}}
function testStream(){const u=document.getElementById('testUrl').value.trim();if(!u)return;const v=document.getElementById('player');v.src=u;v.play().catch(()=>toast('URL loaded'))}
async function refreshData(){try{if(connected)await loadRemoteState();else render();toast('Data refreshed')}catch(e){toast(e.message)}}
function bdDeviceTime(v){try{return new Intl.DateTimeFormat('en-GB',{timeZone:'Asia/Dhaka',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:true}).format(new Date(v))+' BD'}catch{return v||'—'}}
async function loadDevices(){const box=document.getElementById('deviceList');if(!connected){box.textContent='Please login first.';return}box.innerHTML='<div style="padding:18px;text-align:center;color:#9bacbf">Loading devices...</div>';try{const d=await api('/api/admin/devices'),list=Array.isArray(d?.devices)?d.devices:[];const count=document.getElementById('deviceConnected');if(count)count.textContent=list.length;const allowed=document.getElementById('deviceAllowed');if(allowed)allowed.textContent=d?.settings?.deviceLimit??d?.settings?.maxDevices??'—';if(!list.length){box.innerHTML='<div style="padding:18px;color:#9bacbf">No devices registered.</div>';return}box.innerHTML=list.map(x=>{const id=esc(x.deviceId||x.id||''),blocked=x.blocked===true||x.status==='Blocked',name=String(x.userName||x.username||x.user||'Guest'),number=String(x.number||x.mobile||x.phone||'—'),device=String(x.deviceName||x.name||'Mobile Device'),when=x.lastLoginAt||x.lastLogin||x.lastSeen||x.createdAt||'',ip=String(x.ip||x.ipAddress||'—');return `<div class="device-user-card" style="padding:16px 14px;border-bottom:1px solid rgba(120,220,220,.14)"><div style="display:flex;justify-content:space-between;align-items:center;gap:10px;margin-bottom:6px"><b style="font-size:17px;color:#e8f2fc">${esc(name)}</b><b style="font-size:14px;color:${blocked?'#ff5b67':'#39e58c'}">${blocked?'Blocked':'Logged in'}</b></div><div style="line-height:1.65;color:#e7eef8;font-size:14px"><b>Name:</b> ${esc(name)}<br><b>Number:</b> ${esc(number)}<br><b>Device:</b> ${esc(device)}<br><b>Time:</b> ${esc(bdDeviceTime(when))}<br><b>IP:</b> ${esc(ip)}</div><div class="device-actions" style="display:flex;gap:8px;margin-top:11px"><button onclick="${blocked?'unblockDevice':'blockDevice'}('${id}')" style="flex:1;background:${blocked?'#0c2b46':'#b40016'}">${blocked?'Unblock':'Block'}</button><button onclick="deleteDevice('${id}')" style="flex:1">Delete</button></div></div>`}).join('')}catch(e){box.innerHTML='<div style="padding:18px;color:#ff7b86">Could not load devices: '+esc(e.message)+'</div>'}}
async function blockDevice(id){if(!confirm('Block this device?'))return;try{await api('/api/admin/devices/block',{method:'POST',body:JSON.stringify({deviceId:id})});await loadDevices();logAction('Blocked device: '+id)}catch(e){toast(e.message)}}
async function unblockDevice(id){try{await api('/api/admin/devices/unblock',{method:'POST',body:JSON.stringify({deviceId:id})});await loadDevices();logAction('Unblocked device: '+id)}catch(e){toast(e.message)}}
async function deleteDevice(id){if(!confirm('Delete this device?'))return;try{await api('/api/admin/devices?deviceId='+encodeURIComponent(id),{method:'DELETE'});await loadDevices();logAction('Deleted device: '+id)}catch(e){toast(e.message)}}

function showSection(id,fromHistory=false){if(!document.getElementById(id))id='dashboard';document.querySelectorAll('.section').forEach(x=>x.classList.remove('active-section'));document.getElementById(id).classList.add('active-section');document.querySelectorAll('[data-section]').forEach(x=>x.classList.toggle('active',x.dataset.section===id));document.getElementById('sidebar').classList.remove('open');if(id==='devices')loadDevices();if(id==='activity')renderLogs();if(!fromHistory){history.pushState({adminSection:id},'',location.pathname+location.search+'#'+encodeURIComponent(id))}window.scrollTo({top:0,behavior:'smooth'})}
window.addEventListener('popstate',()=>{const id=decodeURIComponent(location.hash.slice(1)||'dashboard');showSection(id,true)});
window.addEventListener('hashchange',()=>{const id=decodeURIComponent(location.hash.slice(1)||'dashboard');showSection(id,true)});

document.querySelectorAll('[data-section]').forEach(b=>b.addEventListener('click',()=>showSection(b.dataset.section)));
document.getElementById('liveGroup').addEventListener('click',()=>document.getElementById('liveSub').classList.toggle('open'));
document.getElementById('menuBtn').addEventListener('click',()=>document.getElementById('sidebar').classList.toggle('open'));
document.getElementById('loginBtn').addEventListener('click',loginWorker);
document.getElementById('workerPassword').addEventListener('keydown',e=>{if(e.key==='Enter')loginWorker()});
document.getElementById('loginEye').addEventListener('click',()=>{const x=document.getElementById('workerPassword');x.type=x.type==='password'?'text':'password'});
document.getElementById('themeBtn').addEventListener('click',()=>document.body.classList.toggle('light'));
document.getElementById('tvM3uFile')?.addEventListener('change',e=>{const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=()=>document.getElementById('tvM3uText').value=r.result;r.readAsText(f)});
document.getElementById('movieM3uFile')?.addEventListener('change',e=>{const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=()=>document.getElementById('movieM3uText').value=r.result;r.readAsText(f)});
document.querySelectorAll('.playlist-tabs').forEach(scope=>scope.querySelectorAll('.tab').forEach(b=>b.addEventListener('click',()=>{scope.querySelectorAll('.tab').forEach(x=>x.classList.toggle('active',x===b));const section=scope.closest('.section');section.querySelectorAll('.import-pane').forEach(x=>x.classList.toggle('active',x.id==='import-'+b.dataset.tab))})));
document.getElementById('editLogoFile').addEventListener('change',e=>{const f=e.target.files[0],img=document.getElementById('editLogoPreview'),msg=document.getElementById('editLogoStatus');if(!f)return;const r=new FileReader();r.onload=()=>{img.src=r.result;img.style.display='block';msg.textContent=f.name+' selected'};r.readAsDataURL(f)});
document.getElementById('editLogo').addEventListener('input',e=>{const u=e.target.value.trim(),img=document.getElementById('editLogoPreview'),msg=document.getElementById('editLogoStatus');if(!u)return;img.src=u;img.style.display='block';msg.textContent='Logo URL selected'});
['dashSearch','dashCat','managerSearch','managerCat','managerStatus'].forEach(id=>{const e=document.getElementById(id);if(e){e.addEventListener('input',render);e.addEventListener('change',render)}});

document.getElementById('channelForm').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.target),c={name:f.get('name'),category:f.get('category'),logo:f.get('logo'),url:f.get('url'),status:f.get('status')};channels.push(c);if(await saveRemoteState()){e.target.reset();render();logAction('Added channel: '+c.name);toast('Channel added')}});
document.getElementById('noticeForm').addEventListener('submit',async e=>{e.preventDefault();const x=Object.fromEntries(new FormData(e.target));try{await saveRemoteState({notice:{...x,enabled:x.enabled==='Yes'},headline:x.text});logAction('Updated website notice');toast('Banner saved')}catch{}});

Object.assign(window,{showSection,loginWorker,logoutWorker,refreshData,preview,editChannel,saveSelectedChannel,deleteChannel,importM3U,importM3UUrl,importXtream,importTvM3U,importTvM3UUrl,importTvXtream,importMovieM3U,importMovieM3UUrl,addCategory,removeCategory,exportData,exportM3U,clearAll,testStream,loadDevices,blockDevice,unblockDevice,deleteDevice,clearLogs});

(async function init(){const lc=JSON.parse(localStorage.getItem('vipChannels')||'null'),lt=JSON.parse(localStorage.getItem('vipTvChannels')||'null'),lm=JSON.parse(localStorage.getItem('vipMovieSeries')||'null'),cat=JSON.parse(localStorage.getItem('vipCategories')||'null');if(Array.isArray(lt))tvChannels=lt.map(norm);if(Array.isArray(lm))movieSeries=lm.map(c=>({...norm(c),category:'MOVIE & SERIES'}));if(!Array.isArray(lt)&&Array.isArray(lc)){tvChannels=lc.filter(c=>String(c.category||'').toUpperCase()!=='MOVIE & SERIES').map(norm);movieSeries=lc.filter(c=>String(c.category||'').toUpperCase()==='MOVIE & SERIES').map(c=>({...norm(c),category:'MOVIE & SERIES'}));}rebuildChannels();if(Array.isArray(cat))categories=cat;render();renderLogs();const initial=decodeURIComponent(location.hash.slice(1)||'dashboard');history.replaceState({adminSection:initial},'',location.pathname+location.search+'#'+encodeURIComponent(initial));showSection(initial,true);try{await api('/api/admin/session');setBackend(true);showLogin(false);await loadRemoteState();await loadDevices()}catch{setBackend(false);showLogin(true)}})();