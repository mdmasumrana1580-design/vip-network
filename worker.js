/**
 * VIP NETWORK - unified Cloudflare Worker
 * Public website + Admin API + KV persistence.
 * Admin and public site use the SAME vip_state_v1 record.
 */
const STATE_KEY='vip_state_v1';
const DEVICES_KEY='vip_devices_v1';
const SETTINGS_KEY='vip_settings_v1';
const SESSION_PREFIX='vip_admin_session:';
const VISITOR_KEY='vip_visitor_stats_v1';
const SESSION_TTL=3600;

const adminPassword=env=>env.ADMIN_PASSWORD||env.ADMIN_PASSWOED||'';
const kv=env=>env.VIP_PLAYLIST||env.PLAYLIST_KV;
const json=(data,status=200,extra={})=>new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store',...extra}});
function corsFor(request){
  const origin=request.headers.get('Origin');
  return {
    'access-control-allow-origin':origin||'*',
    'access-control-allow-methods':'GET,POST,PUT,DELETE,OPTIONS',
    'access-control-allow-headers':'Content-Type, Authorization, X-ViP-Device-ID',
    'access-control-allow-credentials':'true',
    'vary':'Origin'
  };
}
function withCors(r,request){const h=new Headers(r.headers);Object.entries(corsFor(request)).forEach(([k,v])=>h.set(k,v));return new Response(r.body,{status:r.status,statusText:r.statusText,headers:h});}
function detectCategory(name='',group='',meta=''){
  const t=`${name} ${group} ${meta}`.toLowerCase();
  if(/movie|movies|film|films|cinema|cine|vod|video\s*on\s*demand|series|serial|season|episode|episod|s\d{1,2}e\d{1,3}|web\s*(movie|series)|drama/.test(t))return'MOVIE & SERIES';
  if(/sport|cricket|football|fifa|eurosport|willow|ten\s*cricket|ptv\s*sports|tsn|espn|bein|wwe|golf|nfl|nba|star\s*sports|sony\s*sports|t\s*sports/.test(t))return'SPORTS';
  if(/bangla|bangladesh|\bbd\b|somoy|jamuna|ekattor|dbc|maasranga|atn|channel\s*24|news24|independent|ntv|rtv|banglavision|boishakhi|gazi\s*tv|\bgtv\b|b\s*tv|bengal|duronto|deepto|nagorik|mohona|asian\s*tv|desh\s*tv|bijoy\s*tv|mytv|satv|ekushey|bishwa|bangla\s*tv|\bbtv\b|channel\s*i/.test(t))return'BD';
  if(/india|indian|sony|zee|star|colors|\bset\b|\bsab\b|aaj\s*tak|ndtv|republic|news18|times\s*now|india\s*tv|dd\s*(national|sports)|sun\s*tv|asianet|vijay|jaya|starplus|star\s*gold|sony\s*max|sony\s*pix|sony\s*wah|sony\s*yay|sony\s*pal|pictures|b4u|movies\s*now|mnx|hbo\s*india/.test(t))return'INDIA';
  return'OTHER';
}
function norm(c={}){const name=String(c.name||c.title||'Unnamed');const group=String(c.group||c.groupTitle||c.category||'');const category=String(c.category||c.cat||detectCategory(name,group,c.meta||''));return{id:String(c.id||crypto.randomUUID()),name,category,cat:category,logo:String(c.logo||c.tvgLogo||c['tvg-logo']||''),url:String(c.url||c.stream||c.streamUrl||''),status:String(c.status||'Unknown'),enabled:c.enabled!==false};}
function normalizeState(s={}){const channels=Array.isArray(s.channels)?s.channels.map(c=>{const n=norm(c);const cat=detectCategory(n.name,c.group||c.groupTitle||c.category||'',c.meta||'');return{...n,category:cat,cat}}):[];return{...s,channels,categories:Array.isArray(s.categories)&&s.categories.length?s.categories:['ALL','SPORTS','BD','INDIA','OTHER','MOVIE & SERIES'],notice:s.notice&&typeof s.notice==='object'?s.notice:{text:String(s.notice||''),type:'Information',enabled:true},headline:String(s.headline||'')};}
async function readState(env){const store=kv(env);if(!store)throw new Error('VIP_PLAYLIST KV binding is missing');const raw=await store.get(STATE_KEY);if(!raw)return normalizeState({});try{return normalizeState(JSON.parse(raw));}catch{return normalizeState({});}}
async function saveState(env,state){const store=kv(env);if(!store)throw new Error('VIP_PLAYLIST KV binding is missing');const clean=normalizeState(state);await store.put(STATE_KEY,JSON.stringify(clean));return clean;}
async function readDevices(env){const raw=await kv(env).get(DEVICES_KEY);try{return raw?JSON.parse(raw):[]}catch{return[]}}
async function saveDevices(env,list){await kv(env).put(DEVICES_KEY,JSON.stringify(list))}
async function readSettings(env){const raw=await kv(env).get(SETTINGS_KEY);try{return raw?JSON.parse(raw):{deviceLimit:1,accessMode:'approval'}}catch{return{deviceLimit:1,accessMode:'approval'}}}
async function saveSettings(env,data){const s={...(await readSettings(env)),...(data||{})};await kv(env).put(SETTINGS_KEY,JSON.stringify(s));return s}
function cookies(request){const out={};for(const p of(request.headers.get('Cookie')||'').split(';')){const i=p.indexOf('=');if(i>0)out[p.slice(0,i).trim()]=decodeURIComponent(p.slice(i+1).trim())}return out;}
async function authorized(request,env){const expected=adminPassword(env);if(!expected)return false;const auth=request.headers.get('Authorization')||'';if(auth===`Bearer ${expected}`)return true;const token=auth.startsWith('Bearer ') ? auth.slice(7).trim():'';if(token&&await kv(env).get(SESSION_PREFIX+token))return true;const cookie=cookies(request).VIP_ADMIN_SESSION;if(cookie&&await kv(env).get(SESSION_PREFIX+cookie))return true;return false;}
async function requireAdmin(request,env){if(!(await authorized(request,env)))return json({ok:false,error:'Unauthorized'},401);}
function parseM3U(text){const lines=String(text||'').replace(/\r/g,'').split('\n'),out=[];let meta=null;for(const raw of lines){const line=raw.trim();if(!line)continue;if(line.startsWith('#EXTINF')){const comma=line.indexOf(','),name=comma>=0?line.slice(comma+1).trim():'Live Channel';const gm=line.match(/group-title="([^"]*)"/i),lm=line.match(/tvg-logo="([^"]*)"/i);meta={name:name||'Live Channel',group:gm?gm[1]:'',logo:lm?lm[1]:'',meta:line};continue;}if(line.startsWith('#'))continue;if(meta){if(/^(https?|rtmp|rtsp|hls):\/\//i.test(line))out.push(norm({name:meta.name,category:detectCategory(meta.name,meta.group,meta.meta),logo:meta.logo,url:line,status:'Unknown'}));meta=null;}}return out;}
function m3u(channels){return'#EXTM3U\n'+channels.map(c=>`#EXTINF:-1 tvg-logo="${String(c.logo||'').replace(/"/g,'&quot;')}" group-title="${String(c.category||'OTHER').replace(/"/g,'&quot;')}",${String(c.name||'Channel').replace(/\n/g,' ')}\n${c.url}`).join('\n');}
async function visitorStats(env,body={}){const id=String(body.id||'').slice(0,120);if(!id)return{ok:false,error:'visitor id required'};const now=Date.now(),ttl=45000;let s={total:0,visitors:{}};try{const raw=await kv(env).get(VISITOR_KEY);if(raw)s=JSON.parse(raw)}catch{}if(!Number.isFinite(Number(s.total)))s.total=0;if(!s.visitors||typeof s.visitors!=='object')s.visitors={};if(body.newVisit&&!s.visitors[id])s.total++;s.visitors[id]=now;for(const[k,t]of Object.entries(s.visitors))if(!Number.isFinite(Number(t))||now-Number(t)>ttl)delete s.visitors[k];await kv(env).put(VISITOR_KEY,JSON.stringify(s));return{ok:true,total:s.total,online:Object.keys(s.visitors).length};}
async function handleApi(request,env){const url=new URL(request.url),path=url.pathname;if(request.method==='OPTIONS')return withCors(new Response(null,{status:204}),request);
  if(path==='/api/admin/login'&&request.method==='POST'){const b=await request.json().catch(()=>({})),password=String(b.password||'');if(!adminPassword(env)||password!==adminPassword(env))return withCors(json({ok:false,error:'Unauthorized'},401),request);const token=crypto.randomUUID();await kv(env).put(SESSION_PREFIX+token,'1',{expirationTtl:SESSION_TTL});return withCors(json({ok:true,token,expiresIn:SESSION_TTL},200,{'Set-Cookie':`VIP_ADMIN_SESSION=${encodeURIComponent(token)}; Max-Age=${SESSION_TTL}; Path=/; HttpOnly; Secure; SameSite=Lax`}),request);}
  if(path==='/api/admin/session'&&request.method==='GET')return withCors((await authorized(request,env))?json({ok:true,expiresIn:SESSION_TTL}):json({ok:false,error:'Unauthorized'},401),request);
  if(path==='/api/admin/logout'&&request.method==='POST'){const c=cookies(request),auth=(request.headers.get('Authorization')||'');const token=auth.startsWith('Bearer ')?auth.slice(7).trim():c.VIP_ADMIN_SESSION;if(token)await kv(env).delete(SESSION_PREFIX+token);return withCors(json({ok:true},200,{'Set-Cookie':'VIP_ADMIN_SESSION=; Max-Age=0; Path=/; HttpOnly; Secure; SameSite=Lax'}),request);}
  if(path==='/api/state'&&request.method==='GET'){const s=await readState(env);return withCors(json({channels:s.channels||[],categories:s.categories||[],notice:s.notice||{},headline:s.headline||''}),request);}
  if(path==='/api/playlist'&&request.method==='GET'){const s=await readState(env);return withCors(json({channels:s.channels||[]}),request);}
  if(path==='/api/settings'&&request.method==='GET')return withCors(json(await readSettings(env)),request);
  if(path==='/api/visitor'&&request.method==='POST'){const b=await request.json().catch(()=>({}));return withCors(json(await visitorStats(env,b)),request);}
  if(path==='/api/device/register'&&request.method==='POST'){const b=await request.json().catch(()=>({})),id=String(b.deviceId||request.headers.get('X-ViP-Device-ID')||'');if(!id)return withCors(json({ok:false,error:'deviceId required'},400),request);const list=await readDevices(env);let d=list.find(x=>x.deviceId===id);if(!d){d={deviceId:id,name:String(b.name||'Unknown device'),userAgent:String(b.userAgent||request.headers.get('user-agent')||'').slice(0,200),approved:false,status:'Pending',createdAt:new Date().toISOString(),lastSeen:new Date().toISOString()};list.push(d);}else d.lastSeen=new Date().toISOString();await saveDevices(env,list);return withCors(json({ok:true,device:d,settings:await readSettings(env)}),request);}
  if(path==='/api/device/check'&&request.method==='GET'){const id=url.searchParams.get('deviceId')||request.headers.get('X-ViP-Device-ID')||'',list=await readDevices(env),d=list.find(x=>x.deviceId===id);return withCors(json({ok:true,approved:!!d?.approved,device:d||null,settings:await readSettings(env)}),request);}
  const guard=await requireAdmin(request,env);if(guard)return withCors(guard,request);
  if(path==='/api/admin/ping'&&request.method==='GET')return withCors(json({ok:true,connected:true}),request);
  if(path==='/api/admin/state'&&request.method==='GET')return withCors(json({ok:true,state:await readState(env)}),request);
  if(path==='/api/admin/state'&&request.method==='PUT'){const b=await request.json().catch(()=>({})),old=await readState(env),state={...old,...b,channels:Array.isArray(b.channels)?b.channels.map(norm):old.channels||[]};return withCors(json({ok:true,state:await saveState(env,state)}),request);}
  if(path==='/api/admin/playlist'&&request.method==='GET'){const s=await readState(env);return withCors(json({ok:true,channels:s.channels||[]}),request);}
  if(path==='/api/admin/playlist'&&request.method==='PUT'){const b=await request.json().catch(()=>({})),s=await readState(env);s.channels=Array.isArray(b.channels)?b.channels.map(norm):[];return withCors(json({ok:true,state:await saveState(env,s)}),request);}
  if(path==='/api/admin/import-m3u'&&request.method==='POST'){const ct=request.headers.get('content-type')||'';let text='';if(ct.includes('application/json')){const b=await request.json();text=String(b.text||b.m3u||'')}else text=await request.text();const channels=parseM3U(text),s=await readState(env);s.channels=channels;s.categories=[...new Set(channels.map(c=>c.category))];return withCors(json({ok:true,count:channels.length,state:await saveState(env,s)}),request);}
  if(path==='/api/admin/import-m3u-url'&&request.method==='POST'){const b=await request.json().catch(()=>({})),target=String(b.url||'');if(!/^https?:\/\//i.test(target))return withCors(json({ok:false,error:'Invalid M3U URL'},400),request);const r=await fetch(target,{redirect:'follow'});if(!r.ok)return withCors(json({ok:false,error:`M3U URL HTTP ${r.status}`},400),request);const channels=parseM3U(await r.text()),s=await readState(env);s.channels=channels;s.categories=[...new Set(channels.map(c=>c.category))];return withCors(json({ok:true,count:channels.length,state:await saveState(env,s)}),request);}
  if(path==='/api/xtream/import'&&request.method==='POST'){const b=await request.json().catch(()=>({})),server=String(b.server||'').replace(/\/$/,''),user=String(b.username||''),pass=String(b.password||'');if(!server||!user||!pass)return withCors(json({ok:false,error:'server, username and password required'},400),request);const apiUrl=server+'/player_api.php?username='+encodeURIComponent(user)+'&password='+encodeURIComponent(pass)+'&action=get_live_streams',r=await fetch(apiUrl,{redirect:'follow'});if(!r.ok)return withCors(json({ok:false,error:`Xtream HTTP ${r.status}`},400),request);const data=await r.json();if(!Array.isArray(data))return withCors(json({ok:false,error:'Xtream returned invalid data'},400),request);const lim=String(b.limit||'all'),items=lim==='all'?data:data.slice(0,Number(lim)||100),base=server+'/live/'+encodeURIComponent(user)+'/'+encodeURIComponent(pass)+'/',channels=items.map(x=>{const cat=detectCategory(x.name||'',x.category_name||'','');return norm({name:x.name||('Channel '+x.stream_id),category:cat,logo:x.stream_icon||'',url:base+encodeURIComponent(String(x.stream_id))+'.m3u8',status:'Unknown'})}),s=await readState(env);s.channels=channels;s.categories=[...new Set(channels.map(c=>c.category))];return withCors(json({ok:true,count:channels.length,state:await saveState(env,s)}),request);}
  if(path==='/api/admin/check-all'&&request.method==='POST'){const s=await readState(env),checked=await Promise.all((s.channels||[]).map(async c=>{try{const r=await fetch(c.url,{method:'GET',redirect:'follow'});return{...c,status:r.ok?'Active':'Dead'}}catch{return{...c,status:'Dead'}}}));s.channels=checked;return withCors(json({ok:true,count:checked.length,channels:checked,state:await saveState(env,s)}),request);}
  if(path==='/api/admin/settings'&&request.method==='GET')return withCors(json({ok:true,settings:await readSettings(env)}),request);
  if(path==='/api/admin/settings'&&request.method==='PUT')return withCors(json({ok:true,settings:await saveSettings(env,await request.json())}),request);
  if(path==='/api/admin/devices'&&request.method==='GET')return withCors(json({ok:true,devices:await readDevices(env),settings:await readSettings(env)}),request);
  if(path==='/api/admin/devices/approve'&&request.method==='POST'){const b=await request.json().catch(()=>({})),id=String(b.deviceId||''),list=await readDevices(env),d=list.find(x=>x.deviceId===id);if(!d)return withCors(json({ok:false,error:'Device not found'},404),request);d.approved=true;d.status='Approved';await saveDevices(env,list);return withCors(json({ok:true,device:d}),request);}
  if(path==='/api/admin/devices/approve-all'&&request.method==='POST'){const list=await readDevices(env);list.forEach(d=>{d.approved=true;d.status='Approved'});await saveDevices(env,list);return withCors(json({ok:true,count:list.length}),request);}
  if(path==='/api/admin/devices/logout-all'&&request.method==='POST'){const list=await readDevices(env);list.forEach(d=>{d.approved=false;d.status='Logged out'});await saveDevices(env,list);return withCors(json({ok:true}),request);}
  if(path==='/api/admin/devices'&&request.method==='PUT'){const b=await request.json().catch(()=>({})),list=await readDevices(env),id=String(b.deviceId||''),d=list.find(x=>x.deviceId===id);if(!d)return withCors(json({ok:false,error:'Device not found'},404),request);Object.assign(d,b);await saveDevices(env,list);return withCors(json({ok:true,device:d}),request);}
  if(path==='/api/admin/devices'&&request.method==='DELETE'){const id=url.searchParams.get('deviceId')||'',list=(await readDevices(env)).filter(x=>x.deviceId!==id);await saveDevices(env,list);return withCors(json({ok:true}),request);}
  if(path==='/api/admin/export-m3u'&&request.method==='GET')return withCors(new Response(m3u((await readState(env)).channels||[]),{headers:{'content-type':'audio/x-mpegurl; charset=utf-8'}}),request);
  return withCors(json({ok:false,error:'API route not found'},404),request);
}
async function injectPublicSync(request,env,response){const url=new URL(request.url);if(request.method!=='GET'||(url.pathname!=='/'&&url.pathname!=='/index.html'))return response;const ct=response.headers.get('content-type')||'';if(!ct.includes('text/html'))return response;const html=await response.text();const script=`<script>(function(){async function sync(){try{const r=await fetch('/api/state',{cache:'no-store'});if(!r.ok)return;const d=await r.json();const n=d.notice||{},msg=String(n.text||d.headline||'');const el=document.querySelector('.headline-track');if(el&&msg)el.innerHTML='<span>'+msg.replace(/[&<>]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[m]))+'</span><span>'+msg.replace(/[&<>]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[m]))+'</span>';if(n.enabled===false){const h=document.querySelector('.headline');if(h)h.style.display='none';}}catch(e){}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',sync,{once:true});else sync();})();</script>`;return new Response(html.replace('</body>',script+'</body>'),{status:response.status,headers:response.headers});}
export default{async fetch(request,env){try{const url=new URL(request.url);if(url.pathname.startsWith('/api/'))return handleApi(request,env);const response=await env.ASSETS.fetch(request);return injectPublicSync(request,env,response);}catch(e){return withCors(json({ok:false,error:e.message||'Internal error'},500),request);}}};
